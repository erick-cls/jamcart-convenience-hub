
// Dashboard page simplified: renders the main container only now for maintainability.
import DashboardContainer from './dashboard/DashboardContainer';

const Dashboard = () => {
  return <DashboardContainer />;
};

export default Dashboard;
